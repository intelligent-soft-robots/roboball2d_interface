#! /bin/bash

DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
DIR=${DIR}/../python/o80_tennis2d
python3 ${DIR}/run_reality.py & python3 ${DIR}/run_simulation.py & python3 ${DIR}/run_vision.py

